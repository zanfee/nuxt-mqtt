{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "mqtt": "^4.3.7",
    "nuxt": "^3.3.3"
  },
  "dependencies": {
    "consola": "^3.0.1",
    "u8-mqtt": "0.3.2-0"
  }
}
